<%= form_for(position_status) do |f| %>
  <% if position_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position_status.errors.count, "error") %> prohibited this position_status from being saved:</h2>

      <ul>
      <% position_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
